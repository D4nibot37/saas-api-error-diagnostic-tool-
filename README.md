# saas-api-error-diagnostic-tool-
A lightweight command-line tool that **diagnoses common API errors in SaaS/CRM environments** (Salesforce, HubSpot, and generic REST APIs) and returns clear, actionable resolution steps - like a support runbook, but executable
